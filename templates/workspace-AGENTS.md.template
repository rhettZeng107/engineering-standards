# AGENTS.md — <workspace name>

> Keep only project specialization here. Cross-project defaults come from `~/.codex/AGENTS.md`; reusable policy comes from `engineering-standards`.

## Positioning

- Purpose: <one sentence>
- Delivery stage: <requirements | design | implementation | operations>
- Owners: <team or role>

## Repository map

| Path | Role | Remote/branch | Verification |
|---|---|---|---|
| `<repo>` | `<root workspace or nested app>` | `<remote/branch>` | `<build/test command>` |

## Project boundaries

- Source of truth: <schema/service/spec/API>
- Reuse constraints: <existing modules/contracts to extend>
- Do not: <project-specific prohibitions>
- Environment authorization: <what may be changed and what requires approval>

## Workflow specialization

- Task-triggered ADR/spec sources: <paths and when needed; not a mandatory history bundle>
- Semantic navigation: <LSP/tool and solution, when relevant>
- Verification specialization: <project-specific checks; inherit review policy, do not add default reviewers>
- Task records: <docs/tasks/YYYY-MM-DD-topic/ when needed; spec/plan/progress are not a mandatory bundle>
- Authoritative task record: <one progress path for long work, or session plan for short work>
- Continuation entry: <thin pointer to current record, or not needed>
- Delivery order by batch: <local checks, candidate/UAT prerequisites if any, commit/push, CI and final acceptance; do not guess>
- Remote targets: <per-repo authorized targets; no remote means local commit only>
- Optional project-map navigation: <paths if useful; never a blocking or truth-source requirement>

## Verification

- Build: `<command>`
- Test: `<command>`
- API/DB: `<method>`
- UI/E2E: `<command and browser boundary>`
- Git/CI: `<independent completion checks>`
