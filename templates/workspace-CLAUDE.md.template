# CLAUDE.md — <工作区名> 项目特化

> **通用工作流**(三层文档 / Review / E2E 8 项核对 / 鉴权刚性 / SQL / Agent / 双推节奏 / 编码路由 / Teams 委托 等)**以 `~/.claude/CLAUDE.md` 为准,本文件不重复。**
>
> **跨项目工程标准 / ADR / 迁移手册** 见 `engineering-standards` 仓(decisions / standards);本文件只放 **<工作区名> 项目特化** 与**项目级覆盖全局规则**。
>
> 本文件由 `engineering-standards/templates/workspace-CLAUDE.md.template` 实例化(ADR-029)。

---

## 项目概览 / Project Overview

<一句话定位:本工作区交付什么业务>

| 交付线 | Directory | Tech | 端口 |
|---|---|---|---|
| <交付线1> | `<dir>/` | <技术栈> | <端口> |
| <交付线2> | `<dir>/` | <技术栈> | <端口> |

<已下线 / 只读归档目录,若有>

---

## 编码工作流路由 — 项目特化对接

> 全局「按代码类型硬切分 + Teams 委托」规则见 `~/.claude/CLAUDE.md`,本节仅补本工作区落盘锚点。

- 纯前端 → `<前端目录>`
- 后端 → `<后端目录/分层>`
- DB → `<Context / 测试库>`
- 跨前后端契约 → contract-lock 文件作接口

---

## Teams 角色映射 / Teams Role Mapping

| 角色 | 本工作区对接 | 落盘范围 |
|---|---|---|
| `dba` | `<DB Context>` | SQL / 迁移 / Schema |
| `dotnet-developer` / 后端角色 | `<后端目录>` | Controller / Service / Entity / DTO |
| `qwen`(默认前端落盘方) | `<前端目录>` | `.tsx` / `.ts` / `.less` / `.css` |
| `frontend-developer` | `<前端目录>` | 兜底:qwen 失败 / 复杂 UI 重构 / E2E |

---

## Git 双推 / Git Dual Push

**<工作区名> 根** = workspace 容器仓(追踪 `docs/` `scripts/` 根 `*.md`)。
**nested 项目仓** = 各自独立 git,向 <内网远程> + <GitHub/外部远程> 双推。

| 项目目录 | 远程 | 默认双推分支 |
|---|---|---|
| `<dir>/` | <remote> | `master` |

```bash
cd <项目目录> && git push origin master && git push <第二远程> master
```

**注意**:nested repo 的 `.git` 与根 `.git` 隔离,**禁用 submodule**;`.mcp.json`(含密码)**永不入库**。

---

## 构建命令 / Build Commands

```bash
# 后端
<后端 build 命令>
# 前端
<前端 install + dev 命令>
```

---

## CI/CD 与监控 / CI-CD & Monitoring

> 监控双轨见 `engineering-standards` ADR-022;全局 post-push 监控提醒 hook 自动生效(推送后提示起 build 监控)。

- **流水线**:`<各 nested 仓的 CI/CD 平台 + 流水线>`
- **build 监控**(主动):`docs/ops/<监控脚本>` —— 双推后起后台监控,绿静默推进 / 红汇报并启自治修复
- **自治修复 SOP**:`docs/ops/cicd-self-heal-sop.md`(三层分流)

---

## 架构 / Architecture

- **<后端>**:<分层 / 关键路径>
- **EF Context / DB**:`<路径>`
- **Controllers**:`<路径>`
- **Policy 注册**:`<路径>`

---

## 鉴权落点 / Auth Chain(若涉及鉴权)

> 全局「鉴权 4 条刚性」在本工作区的实现位置:

1. `[Authorize]` 默认属性 — `<Controller 目录>`
2. Policy `AddPolicy(...)` 注册 — `<Program.cs 路径>`
3. 权限码 + 菜单种子 — `<真理源表>`
4. SSO token — `<登录链路>`

---

## 前端 UI 标准 / 子应用接入(若适用)

> 前端列表页 / 表单默认套 `engineering-standards/standards/frontend-ui-standard.md`(前端统一 4 标准)。
> 嵌入业务门户的子应用按 `engineering-standards/standards/subapp-onboarding-guide.md` 接入。

---

## 项目级覆盖全局规则速查 / Project-Level Overrides

> 仅列本工作区**覆盖/特化全局规则**的条目;无则留空。

| 项目特化 | 落点摘要 | memory |
|---|---|---|
| <特化项> | <摘要> | `<memory 文件>` |

---

## 重要决策记录 / ADR

> **跨项目 ADR**:`engineering-standards/decisions/`(单一真理源)。
> **本工作区项目特化 ADR**:`docs/decisions/ADR-NNN-<topic>.md`(仅横向影响 ≥ 2 spec / 推翻先前规则 才落)。
> 触发条件 / 治理铁律见全局 `~/.claude/CLAUDE.md`「四层文档」段。

<本工作区现有 ADR 列表>

---

## 文档目录规范 / Doc Convention

> 完整规范见 `engineering-standards/standards/doc-conventions.md`。
> `docs/superpowers/specs/<YYYY-MM-DD-topic>/` 与 `plans/<同名>/` 单目录;新建必走单目录。

---

## 测试环境 / Test Environment

- **测试账号**:`<账号>` / `<密码>`,`<登录地址>`
- **数据库**:`<测试库>` / `<生产库>`

---

## 重要提示 / Important Notes

- <本工作区需要 Claude 长期记住的特化提示:已下线模块、技术栈差异、推送分支约束等>

---

## 老项目迁移工作区附加(若本工作区是迁移改造)

> 见 `engineering-standards/standards/legacy-migration-playbook.md` + ADR-028。
- **基线声明**:后端运行时 `<>` / 前端工具链 `<>` / 前端工程标准 `<>`。
- **源基线分支**:`<源仓 + 分支>`。
- **STEP 1 启动第一步**:产源工件清单(逐页逐接口标 完好/半成品/坏)。
